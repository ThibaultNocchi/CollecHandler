overwrite: true
schema: "../schema.graphql"
documents: "src/graphql/*.graphql"
generates:
  src/graphql/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-vue-urql"
